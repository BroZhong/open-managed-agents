# oma-cli verification — 2026-09-23

Scope: #180–#196 on baseline `5b81574adf03411039cf735746259698113f6fcb`.
Only CLI, its tests and documentation are changed; existing Host logic is retained.
No npm publication or deployment was performed.

## Completed acceptance

- Built TypeScript CLI and independently installed `oma-cli-local@0.1.0` tarball;
  executable `oma-cli`, offline help/version/schema/guide, and isolated HTTP
  Agent list → get succeeded without runtime source/development dependencies.
- Deterministic CLI tests exercise public input/output/HTTP/SSE boundaries.
- Existing SessionRouter `turn-idle-lifecycle.test.ts`: 6 tests passed, including
  completion-before-acknowledgement, queued Turns, Interrupt and claimed head.
- Real repository Host + SessionRouter integration: CLI send/wait passed while
  a second input was injected after durable completion but before acknowledgement.
  Both completion events and exactly one final idle were observed; only the last
  Turn's two text blocks were returned.

## Authorized live acceptance

An independently installed tarball ran against `https://agentry.welltop.tech/api`
on Node.js v23.7.0. Credentials came from the supplied environment and were not
persisted. Only new resources with prefix `oma-cli-check-1790166553570` were mutated.

Created fixture identities:

- Agent: `agent_K_Gy0QEAhfhrGngQFoXQl`
- Workspace: `ws_YvPMZg41w24GRqwpiTB-l`
- Session: `sess_OMv1N1sOGHM0byh87dZ_W`
- Library Skill: `skill_acHplaPg6nIxZE8WuxxNt`

Verified workflows:

1. Agent creation, Agent File write/list/read and independent Agent fork.
2. Library upload → Equip → independent fork ID → private file edit → export;
   the Library tree did not gain the fork's private file.
3. Workspace text upload and binary upload/download; exact bytes matched.
4. Two accepted inputs → send --wait --stream → one final result → independent
   wait replay of the last Turn. Durable history contained both completion events
   and an idle event after the last completion.
5. Event list/detail/follow; follow emitted only event records, with no result.
6. Agent-created output.txt downloaded and independently compared with the exact
   UTF-8 bytes `OMA_CLI_ACCEPTED\n`. Successful wait alone was not used as proof.

Cleanup succeeded: test Session hidden; four Workspace files deleted, then
Workspace hidden; test Agent forks unequipped; Agent Files and both test Agents
deleted; Library Skill deleted. Session/Workspace soft deletion retains records
and history; no claim of storage erasure or undo. No existing resource was edited.

## Reproduction and limits

Run `node cli/scripts/verify-package.mjs` for local package verification.
Run `node cli/scripts/live-acceptance.mjs` only with explicit environment credentials
and authorization to create isolated test resources. It refuses missing connection
configuration and never defaults to a production endpoint.

The live workflow verifies the deployed path; deterministic fixtures inject
otherwise disruptive network, timeout, symlink, overwrite and replay failures.
The repository Host integration verifies the #179 transaction boundary in memory;
it is not a proof of every production PostgreSQL interleaving. Doctor cannot
infer that transaction implementation from OpenAPI. No platform other than the
recorded macOS runtime has been exercised; registry publication remains separate.

## Final artifact and checks

- Final CLI suite on **Node.js v22.0.0**: **23/23 passed**, zero skipped.
  Includes two real repository Host integrations and HTTP/SSE failure injection.
- TypeScript typecheck/build passed. Existing #179 lifecycle suite: **6/6 passed**.
- Independent install verification passed on Node.js **v22.0.0** (declared minimum)
  and **v23.7.0**. No Windows/Linux installation claim is made.
- Tarball: `oma-cli-local-0.1.0.tgz`, 19 packaged files, zero runtime dependencies.
- SHA-256 (packed with Node.js 22.0.0):
  `6644584280f9791169948f8a5edd36660b0d9ef8ae1feca1653fa9308ea6a5cc`.
  The retained artifact digest matches the independently installed/tested tarball.
- Package inspection found no API-key patterns, private developer paths, tests,
  live scripts, environment files or development dependencies.
- The live workflow above ran before review hardening. Final retry/deadline and
  local validation changes passed deterministic Node 22 regressions and package
  verification; no second production run was needed for those client-only changes.

## Standards

Review baseline: `5b81574`; implementation: `2856dd7`. Initial review and recheck
reported four findings; all are resolved in `bd6aab2` and `de6da69`:

1. Workspace transfer source validation occurred after a Host metadata read.
   Moved the guard after local validation; missing source now sends zero requests.
2. Workspace schema omitted prerequisite metadata/list reads. Registered those
   routes with the commands and included rename source/target lookups.
3. Long accepted durations overflowed Node timers. Added rescheduled deadlines
   for wait/follow, HTTP timeout and SSE inactivity; 720h remains active until
   SIGINT without an overflow warning.
4. Recheck found Skill export directory validation also happened after metadata
   read. It now precedes the request; regression verifies zero Host requests.

The reviewer confirmed all four resolved. No outstanding standards findings.

## Spec

The independent review against #180–#196 found four issues; all resolved and
confirmed by focused recheck:

1. Wait stopped at a transient history/status read failure. Bounded GET recovery
   now preserves the durable cursor and overall deadline, without resubmitting.
2. Long wait/follow durations expired immediately due to timer overflow. The
   shared deadline implementation and long-duration regressions correct this.
3. Invalid Workspace upload paths caused networking before local failure. Local
   validation now precedes all Host requests.
4. Equip rejected an empty flag ID but accepted an empty body ID, including in
   dry-run. Both sources now fail the same validation before networking.

The reviewer verified timeout during retry backoff and SIGINT during a long follow.
No outstanding spec findings within the review scope. Final counts: Standards
4 resolved / 0 outstanding; Spec 4 resolved / 0 outstanding. No remaining worst
issue on either axis.

## Issue coverage map

| Issues | Implementation and evidence |
| --- | --- |
| #180–#182 | Agent discovery/configuration/Files, public protocol tests, installed list/get, live Files/Fork |
| #183–#185 | Workspace metadata/text/byte transfer, signed credential isolation, preflight/partial-failure tests, live binary and artifact checks |
| #186–#189 | Library/upload/export/Equip/file edits, actual Host fork/overwrite/ownership workflows, live private copy export |
| #190–#193 | Session management/input/Complete Events/wait, SSE cursor recovery, actual Host ingress/ack boundary, live queued Turns and final output |
| #194 | Shared executable schema/help metadata, offline guide/version, diagnostic partial checks |
| #195–#196 | Independent package, minimum-runtime verification, live acceptance and cleanup, design/guidance/coverage records |
