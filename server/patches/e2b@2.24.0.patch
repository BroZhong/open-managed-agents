diff --git a/dist/index.js b/dist/index.js
--- a/dist/index.js
+++ b/dist/index.js
@@ -1430,7 +1430,7 @@
    * @internal
    * @access protected
    */
-  constructor(pid, handleDisconnect, handleKill, events, onStdout, onStderr, onPty) {
+  constructor(pid, handleDisconnect, handleKill, events, onStdout, onStderr, onPty, onStdoutBytes, onStderrBytes) {
     this.pid = pid;
     this.handleDisconnect = handleDisconnect;
     this.handleKill = handleKill;
@@ -1438,6 +1438,8 @@
     this.onStdout = onStdout;
     this.onStderr = onStderr;
     this.onPty = onPty;
+    this.onStdoutBytes = onStdoutBytes;
+    this.onStderrBytes = onStderrBytes;
     this._stdout = "";
     this._stderr = "";
     this._wait = this.handleEvents();
@@ -1511,6 +1513,8 @@
   iterateEvents() {
     return __asyncGenerator(this, null, function* () {
       var _a3;
+      const stdoutDecoder = new TextDecoder("utf-8", { ignoreBOM: true });
+      const stderrDecoder = new TextDecoder("utf-8", { ignoreBOM: true });
       try {
         for (var iter = __forAwait(this.events), more, temp, error; more = !(temp = yield new __await(iter.next())).done; more = false) {
           const event = temp.value;
@@ -1520,12 +1524,14 @@
             case "data":
               switch (e.value.output.case) {
                 case "stdout":
-                  out = new TextDecoder().decode(e.value.output.value);
+                  this.onStdoutBytes?.(e.value.output.value);
+                  out = stdoutDecoder.decode(e.value.output.value, { stream: true });
                   this._stdout += out;
                   yield [out, null, null];
                   break;
                 case "stderr":
-                  out = new TextDecoder().decode(e.value.output.value);
+                  this.onStderrBytes?.(e.value.output.value);
+                  out = stderrDecoder.decode(e.value.output.value, { stream: true });
                   this._stderr += out;
                   yield [null, out, null];
                   break;
@@ -1535,6 +1541,16 @@
               }
               break;
             case "end":
+              out = stdoutDecoder.decode();
+              if (out) {
+                this._stdout += out;
+                yield [out, null, null];
+              }
+              out = stderrDecoder.decode();
+              if (out) {
+                this._stderr += out;
+                yield [null, out, null];
+              }
               this.result = {
                 exitCode: e.value.exitCode,
                 error: e.value.error,
@@ -3522,7 +3538,9 @@
         events,
         opts == null ? void 0 : opts.onStdout,
         opts == null ? void 0 : opts.onStderr,
-        void 0
+        void 0,
+        opts == null ? void 0 : opts.onStdoutBytes,
+        opts == null ? void 0 : opts.onStderrBytes
       );
     } catch (err) {
       cleanup();
@@ -3573,7 +3591,9 @@
         events,
         opts == null ? void 0 : opts.onStdout,
         opts == null ? void 0 : opts.onStderr,
-        void 0
+        void 0,
+        opts == null ? void 0 : opts.onStdoutBytes,
+        opts == null ? void 0 : opts.onStderrBytes
       );
     } catch (err) {
       cleanup();
diff --git a/dist/index.mjs b/dist/index.mjs
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -1374,7 +1374,7 @@
    * @internal
    * @access protected
    */
-  constructor(pid, handleDisconnect, handleKill, events, onStdout, onStderr, onPty) {
+  constructor(pid, handleDisconnect, handleKill, events, onStdout, onStderr, onPty, onStdoutBytes, onStderrBytes) {
     this.pid = pid;
     this.handleDisconnect = handleDisconnect;
     this.handleKill = handleKill;
@@ -1382,6 +1382,8 @@
     this.onStdout = onStdout;
     this.onStderr = onStderr;
     this.onPty = onPty;
+    this.onStdoutBytes = onStdoutBytes;
+    this.onStderrBytes = onStderrBytes;
     this._stdout = "";
     this._stderr = "";
     this._wait = this.handleEvents();
@@ -1455,6 +1457,8 @@
   iterateEvents() {
     return __asyncGenerator(this, null, function* () {
       var _a3;
+      const stdoutDecoder = new TextDecoder("utf-8", { ignoreBOM: true });
+      const stderrDecoder = new TextDecoder("utf-8", { ignoreBOM: true });
       try {
         for (var iter = __forAwait(this.events), more, temp, error; more = !(temp = yield new __await(iter.next())).done; more = false) {
           const event = temp.value;
@@ -1464,12 +1468,14 @@
             case "data":
               switch (e.value.output.case) {
                 case "stdout":
-                  out = new TextDecoder().decode(e.value.output.value);
+                  this.onStdoutBytes?.(e.value.output.value);
+                  out = stdoutDecoder.decode(e.value.output.value, { stream: true });
                   this._stdout += out;
                   yield [out, null, null];
                   break;
                 case "stderr":
-                  out = new TextDecoder().decode(e.value.output.value);
+                  this.onStderrBytes?.(e.value.output.value);
+                  out = stderrDecoder.decode(e.value.output.value, { stream: true });
                   this._stderr += out;
                   yield [null, out, null];
                   break;
@@ -1479,6 +1485,16 @@
               }
               break;
             case "end":
+              out = stdoutDecoder.decode();
+              if (out) {
+                this._stdout += out;
+                yield [out, null, null];
+              }
+              out = stderrDecoder.decode();
+              if (out) {
+                this._stderr += out;
+                yield [null, out, null];
+              }
               this.result = {
                 exitCode: e.value.exitCode,
                 error: e.value.error,
@@ -3479,7 +3495,9 @@
         events,
         opts == null ? void 0 : opts.onStdout,
         opts == null ? void 0 : opts.onStderr,
-        void 0
+        void 0,
+        opts == null ? void 0 : opts.onStdoutBytes,
+        opts == null ? void 0 : opts.onStderrBytes
       );
     } catch (err) {
       cleanup();
@@ -3530,7 +3548,9 @@
         events,
         opts == null ? void 0 : opts.onStdout,
         opts == null ? void 0 : opts.onStderr,
-        void 0
+        void 0,
+        opts == null ? void 0 : opts.onStdoutBytes,
+        opts == null ? void 0 : opts.onStderrBytes
       );
     } catch (err) {
       cleanup();
diff --git a/dist/index.d.ts b/dist/index.d.ts
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -4115,6 +4115,8 @@
     private readonly onStdout?;
     private readonly onStderr?;
     private readonly onPty?;
+    private readonly onStdoutBytes?;
+    private readonly onStderrBytes?;
     private _stdout;
     private _stderr;
     private result?;
@@ -4125,7 +4127,7 @@
      * @internal
      * @access protected
      */
-    constructor(pid: number, handleDisconnect: () => void, handleKill: () => Promise<boolean>, events: AsyncIterable<ConnectResponse | StartResponse>, onStdout?: ((stdout: string) => void | Promise<void>) | undefined, onStderr?: ((stderr: string) => void | Promise<void>) | undefined, onPty?: ((pty: Uint8Array) => void | Promise<void>) | undefined);
+    constructor(pid: number, handleDisconnect: () => void, handleKill: () => Promise<boolean>, events: AsyncIterable<ConnectResponse | StartResponse>, onStdout?: ((stdout: string) => void | Promise<void>) | undefined, onStderr?: ((stderr: string) => void | Promise<void>) | undefined, onPty?: ((pty: Uint8Array) => void | Promise<void>) | undefined, onStdoutBytes?: ((data: Uint8Array) => void | Promise<void>) | undefined, onStderrBytes?: ((data: Uint8Array) => void | Promise<void>) | undefined);
     /**
      * Command execution exit code.
      * `0` if the command finished successfully.
@@ -7137,10 +7139,14 @@
      * Callback for command stdout output.
      */
     onStdout?: (data: string) => void | Promise<void>;
+    /** Original protobuf output bytes, delivered before text decoding. */
+    onStdoutBytes?: (data: Uint8Array) => void | Promise<void>;
     /**
      * Callback for command stderr output.
      */
     onStderr?: (data: string) => void | Promise<void>;
+    /** Original protobuf output bytes, delivered before text decoding. */
+    onStderrBytes?: (data: Uint8Array) => void | Promise<void>;
     /**
      * If true, command stdin is kept open and you can send data to it using {@link Commands.sendStdin} or {@link CommandHandle.sendStdin}.
      * @default false
@@ -7156,7 +7162,7 @@
 /**
  * Options for connecting to a command.
  */
-type CommandConnectOpts = Pick<CommandStartOpts, 'onStderr' | 'onStdout' | 'timeoutMs'> & CommandRequestOpts;
+type CommandConnectOpts = Pick<CommandStartOpts, 'onStderr' | 'onStdout' | 'onStderrBytes' | 'onStdoutBytes' | 'timeoutMs'> & CommandRequestOpts;
 /**
  * Information about a command, PTY session or start command running in the sandbox as process.
  */
diff --git a/dist/index.d.mts b/dist/index.d.mts
--- a/dist/index.d.mts
+++ b/dist/index.d.mts
@@ -4115,6 +4115,8 @@
     private readonly onStdout?;
     private readonly onStderr?;
     private readonly onPty?;
+    private readonly onStdoutBytes?;
+    private readonly onStderrBytes?;
     private _stdout;
     private _stderr;
     private result?;
@@ -4125,7 +4127,7 @@
      * @internal
      * @access protected
      */
-    constructor(pid: number, handleDisconnect: () => void, handleKill: () => Promise<boolean>, events: AsyncIterable<ConnectResponse | StartResponse>, onStdout?: ((stdout: string) => void | Promise<void>) | undefined, onStderr?: ((stderr: string) => void | Promise<void>) | undefined, onPty?: ((pty: Uint8Array) => void | Promise<void>) | undefined);
+    constructor(pid: number, handleDisconnect: () => void, handleKill: () => Promise<boolean>, events: AsyncIterable<ConnectResponse | StartResponse>, onStdout?: ((stdout: string) => void | Promise<void>) | undefined, onStderr?: ((stderr: string) => void | Promise<void>) | undefined, onPty?: ((pty: Uint8Array) => void | Promise<void>) | undefined, onStdoutBytes?: ((data: Uint8Array) => void | Promise<void>) | undefined, onStderrBytes?: ((data: Uint8Array) => void | Promise<void>) | undefined);
     /**
      * Command execution exit code.
      * `0` if the command finished successfully.
@@ -7137,10 +7139,14 @@
      * Callback for command stdout output.
      */
     onStdout?: (data: string) => void | Promise<void>;
+    /** Original protobuf output bytes, delivered before text decoding. */
+    onStdoutBytes?: (data: Uint8Array) => void | Promise<void>;
     /**
      * Callback for command stderr output.
      */
     onStderr?: (data: string) => void | Promise<void>;
+    /** Original protobuf output bytes, delivered before text decoding. */
+    onStderrBytes?: (data: Uint8Array) => void | Promise<void>;
     /**
      * If true, command stdin is kept open and you can send data to it using {@link Commands.sendStdin} or {@link CommandHandle.sendStdin}.
      * @default false
@@ -7156,7 +7162,7 @@
 /**
  * Options for connecting to a command.
  */
-type CommandConnectOpts = Pick<CommandStartOpts, 'onStderr' | 'onStdout' | 'timeoutMs'> & CommandRequestOpts;
+type CommandConnectOpts = Pick<CommandStartOpts, 'onStderr' | 'onStdout' | 'onStderrBytes' | 'onStdoutBytes' | 'timeoutMs'> & CommandRequestOpts;
 /**
  * Information about a command, PTY session or start command running in the sandbox as process.
  */
