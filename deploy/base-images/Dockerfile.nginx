ARG UPSTREAM_IMAGE=nginx:1.27-alpine
FROM ${UPSTREAM_IMAGE}

LABEL org.opencontainers.image.title="OMA Nginx runtime base" \
      org.opencontainers.image.description="Pinned Nginx runtime base for the OMA web console"
